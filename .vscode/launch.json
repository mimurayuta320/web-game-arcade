{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Web Game in Chrome (Dev)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173/",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Run Web Game Dev Server"
    }
  ]
}
